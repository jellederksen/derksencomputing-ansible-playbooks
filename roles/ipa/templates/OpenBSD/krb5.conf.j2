#Ansible information:
#    Filename: {{ template_path | regex_replace(".*/ansible[^/]*", "..") }}
#    Filedate: {{ template_mtime | regex_replace("\..*$", "")  }}
#    Hostname: {{ template_host }}
# $OpenBSD: krb5.conf,v 1.1 2014/07/13 14:10:13 ajacoutot Exp $

# See krb5.conf(5) and the heimdal info(1) page for more information.

[libdefaults]
	# local realm(s)
	default_realm = GALAXY.JDCOMPUTING.NL
	dns_lookup_kdc = true
	dns_lookup_realm = true

[realms]
	GALAXY.JDCOMPUTING.NL = {
		# list of KDC(s) for this realm
		#kdc = kerberos.domain.tld

		# admin server for this realm
		#admin_server = kerberos.domain.tld
	}

[kadmin]
	# default salt string
	default_keys = v5

[logging]
	# log to syslog(3)
	kdc = SYSLOG:INFO:DAEMON
	kpasswdd = SYSLOG:INFO:AUTH
	default = SYSLOG:INFO:DAEMON

#------------------------------------------------------------------------------
#Copyright (c) 2017 by {{ name }} - {{ company }}
#------------------------------------------------------------------------------
